import { StatementsClient } from "@/components/StatementsClient";

// Statement upload + import status. Auth is enforced by middleware.
export default function StatementsPage() {
  return <StatementsClient />;
}

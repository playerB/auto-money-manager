# Dependencies for the GitHub Actions processing job only (kept minimal/fast).
supabase>=2.4
python-dotenv>=1.0
tzdata>=2024.1
pytesseract>=0.3.10
pillow>=10.0
# PDF statement import (bank/credit-card statements).
pdfplumber>=0.11
# Needed by pdfminer.six to open AES-encrypted PDFs (KBANK e-statements).
cryptography>=42
